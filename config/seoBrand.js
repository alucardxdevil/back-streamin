/** Re-exporta la config SEO compartida (crawlers /api/og/*). */
export {
  BRAND_NAME,
  BRAND_DOMAIN,
  TWITTER_HANDLE,
  SEO_DEFAULT_TITLE,
  SEO_DEFAULT_DESCRIPTION,
  SEO_DEFAULT_KEYWORDS,
  videoDescription,
  profileDescription,
  profileDescriptionNoBio,
  creatorBioFallback,
  assertNoLegacyBrand,
} from '../../shared/seoBrand.js'
